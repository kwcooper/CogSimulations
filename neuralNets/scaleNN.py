#neural net that can scale the number of nodes, edges, and layers

# probably the best bet is to add each of the matricies to a data struct?
# but how will we handle for the connectome?

# what would the connectome of a multilayer net look like?

connectome = np.sparse(1,1)


for i in range(0,len(connectome):
    for j in range(0, len(i):
        if connectome[i][j] == 1:
            

